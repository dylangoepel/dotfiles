#!/bin/bash

volume="$(wpctl get-volume @DEFAULT_AUDIO_SINK@ | sed 's/Volume: //')"
voll="$(echo $volume | sed 's/ .*$//')"
vol=$(echo "$voll" | tr -d \.)
if [ "$volume" = "$voll" ]; then
    dunstify -t 500 -u low "" -h int:value:$vol
else
    dunstify -t 1000 -u low "" -h int:value:$vol
fi
